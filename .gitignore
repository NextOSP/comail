# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri updater signing keys (never commit)
src-tauri/.tauri/

# Local packaging / staging output (installers are built by CI)
build/
*.AppImage

# Local per-developer macOS dev code-signing (see src-tauri/scripts/macos-dev-cert.sh)
src-tauri/.cargo/config.toml

# Embedding model weights (~130MB): downloaded on demand at runtime, never committed
src-tauri/resources/models/
